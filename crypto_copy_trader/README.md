# Crypto Copy Trader

Automated crypto copy trading pipeline that detects on-chain signals and mirrors them on Binance.

```bash
python -m venv .venv
pip install -r requirements.txt
cp .env.example .env
pytest
python main.py
```
